hotfix: 5 步真 verify 发现 P0/P1 5 项问题（按千问新指引）

背景：千问对账我的 A 桶部署 5 张真实截图，发现我的"5 步 curl 200 + grep"流水线
漏掉了"白字白底 / 假数据 / winner 打架"3 类致命问题。本次 hotfix 只修回归 + 数据硬伤，
不加任何新视觉特性；B 桶（reveal/glow）暂停等验证后再启动。

P0‑1 顶栏深色面 !important 兜底
  现状 globals.css .site-header 已是深色底 linear-gradient(c-deep, c-primary)
  加 !important 兜底防 purge / 任意外部覆盖（user 截图报告顶栏蒸发可能来自
  browser cache / mobile 视口截图，但加 !important 不破坏现状）
  文件: src/app/globals.css line 101

P0‑2 EvidenceCard 改实色深底 + !important
  原 .evidence-card 用 background: rgba(255,255,255,0.08)（8% 透明白底）
  在浅色 hero 渐变上对比度不足，user 报告白字白底隐形
  改为 background: var(--c-deep) 实色 + !important + 边框 + 阴影
  文件: src/app/globals.css line 1715

P1‑1 进度条 8.8/8.2 假数据全删
  根因：107 篇 reviews 全无 printCompatibility/ecommerceFit 字段
  我之前在 page.js 硬编码 8.8/8.2 是凭空捏造数据
  修法：直接删除 ReviewCard 的 vertical-score 整块渲染（CSS 保留在 globals.css 备用）
  未来真实字段补齐 + verify-cron 加"进度条全雷同"断言 后再绑字段
  文件: src/app/page.js ReviewCard 函数
  验证: out/index.html vertical-score matches = 0 ✓

P1‑2 修 winner 错配（printify-vs-gelato）
  根因：我 hardcode 'printify-vs-gelato': 'B'（Gelato 赢），但 conclusion
  首句 "Choose Printify if you want the largest product catalog (1,300+ items)..."
  → 徽章与正文互扇耳光
  修法：COMPARISON_WINNERS 改 'printify-vs-gelato': 'A'（Printify 赢）
  文件: src/app/page.js line 12-19
  验证: out/index.html 5 vs-card-a-wins + 1 vs-card-b-wins = 6（修复前是 4+2）
  + 6 winner-badge 全部对齐 conclusion

P1‑3 affiliate-tag 挪到 card 右上角（从 cta 行内挪出）
  根因：affiliate-tag 在 card-cta-group 内与 Read Review/Check Deal 争横向空间
  → 320px 宽时 Check Deal 折行成竖窄块
  修法：.affiliate-tag { position: absolute; top: 14px; right: 14px; } + .review-card { position: relative; }
       .card-cta-deal { flex: 1 1 auto; white-space: nowrap; }（不折行）
       .card-cta-read { flex: 0 0 auto; white-space: nowrap; }
  文件: src/app/globals.css line 1874 起 / src/app/page.js ReviewCard
  验证: 71 affiliate-tag + 71 card-cta-deal 渲染（数量匹配 71 个有 affiliateUrl 的工具）

未做（按 user 拍板"先 1"原则）：
  - P2‑1 杀死亡空白带 → P2 收尾推
  - P2‑2 标签三态 → P2 收尾推
  - P2‑3 胜方高亮块对齐 → P2 收尾推
  - P2‑4 section 背景规律 → P2 收尾推
  - verify 升级 (白字白底 + 进度条全雷同 + winner/conclusion 断言) → 单独 cron
  - B 桶 reveal/glow 动效 → 暂停等 hotfix 验证通过后启动

验证：npm run build 139/139 PASS（页数不变），5 步真 verify
（grep winner-badge / vertical-score / affiliate-tag counts）全过。
