# Cloudflare Pages configuration for aitoptools.net
# 2026-07-15: 添加 wrangler.toml 以便 fallback deploy via wrangler CLI
# 主要部署方式（推荐）: git push → CF Pages Git 集成自动构建
# 备用方式: npx wrangler pages deploy out --project-name aitoptools --branch main

name = "aitoptools"
compatibility_date = "2026-07-15"
pages_build_output_dir = "out"

[env.production]
