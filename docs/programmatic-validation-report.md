# Programmatic SEO — Step 1 Data Layer Validation Report

> Date: 2026-08-03
> Pipeline: aitoptools.net 200-page programmatic SEO (M3 deliverables → Hermes generator)
> SSoT files: `data/keywords-200.csv` (199 data rows) + `data/internal-links-200.json` (199 keys)

## 1. Input inventory

| Artifact | File | Expected | Actual | Status |
|---|---|---|---|---|
| Keyword CSV | `data/keywords-200.csv` | 200 rows | 199 data rows (+1 header) | ⚠️ 199, not 200 |
| Internal-link matrix | `data/internal-links-200.json` | 200 slugs × 3 links | 199 keys, all exactly 3 | ✅ |
| Page master drafts | `.hermes/drafts/programmatic-seo/` | 3 files × N | 597 files (= 199 × 3) | ✅ |
| Architecture doc | `docs/SEO_ARCHITECTURE.md` | v1.0 | present | ✅ |

> Note: CSV is named `keywords-200` but contains 199 data rows. JSON and drafts agree at 199.
> The pipeline generates 199 pages (not 200). M3 should confirm whether 1 row is missing.

## 2. Validation checks (all passed)

### 2.1 Slug uniqueness — ✅
- 199 slugs, zero duplicates.

### 2.2 Slug charset — ✅
- All slugs match `^[a-z0-9]+(-[a-z0-9]+)*$` (kebab-case, lowercase, no illegal chars).
- Max slug length: 43 chars (limit 80). No slug > 80.

### 2.3 Internal links — ✅
- Every CSV slug exists as a key in `internal-links-200.json` (199/199).
- Every key has exactly **3** internal links (199/199).
- No CSV slug missing from JSON; no orphan JSON keys.
- All 3 links per row resolve to slugs in the same file (self-contained matrix).

### 2.4 Tool pool — ✅
- All `primary_tool` values ∈ 9-tool pool: printify, printful, gelato, kittl, creative-fabrica, claid, mockey, picjam, greenonion.
- All `secondary_tools` (pipe-separated) ∈ same pool.

### 2.5 Primary-tool distribution (M3 target ≥15 each) — ✅

| Tool | Count |
|---|---|
| printful | 25 |
| kittl | 25 |
| claid | 25 |
| printify | 24 |
| mockey | 22 |
| creative-fabrica | 22 |
| gelato | 19 |
| picjam | 19 |
| greenonion | 18 |
| **Total** | **199** |

## 3. Warnings (data-quality, non-blocking)

### W1 — primary_tool also appears in secondary_tools (41 slugs)
e.g. `best-ai-tools-for-print-on-demand` has primary `gelato`, secondary `printful|gelato`.
Generator handles this by **deduplicating** the featured tool list (primary first, then
secondaries in CSV order, skipping repeats). Comparison table columns dedupe too.

### W2 — CSV row count is 199, not 200
File name says 200; M3 to confirm if one row was dropped. Pipeline targets whatever rows exist.

### W3 — picjam / greenonion have no review data in `reviews.json`
They get `has_review: false`, no `rating`/`price` (no fabrication per SEO_ARCHITECTURE §0).
Their affiliate URLs are PENDING → CTA links are skipped (no fake `?fpr=` or placeholder URLs).

## 4. Verdict

**ALL BLOCKING CHECKS PASSED.** Data layer ready for Step 2 (page generation).

---
Generated by Hermes (Step 1 of programmatic SEO build-out, 2026-08-03)
